[Open in Codespaces](https://codespaces.new/mandrasch/ddev-codespaces-test)


Launch:

- `ddev start && ddev launch`

Currently broken -> Gateway timeout?

Setup:

```bash
ddev config --project-type=php

```
